<!DOCTYPE html>
<html lang="en" class="h-100">
{{>header}}
<body class="bg-dark text-light h-100">
<h1 class="display-3 mt-5 mb-4 text-center">Hello, {{user.name}}!</h1>
<hr/ class="half-size">
<p class="lead mt-4 mb-5 text-center">
  Create, update, delete assignments for your students.
</p>
<div class="container-xl bg-light text-dark w-50 mt-5 p-5 shadow rounded">
  <div class="d-flex justify-content-between align-items-start mb-4">
    <div>
      <button type="button" class="btn btn-outline-primary d-flex align-items-center" data-bs-toggle="modal" data-bs-target="#addAssignment">
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-plus-lg me-1" viewBox="0 0 16 16">
        <path d="M8 0a1 1 0 0 1 1 1v6h6a1 1 0 1 1 0 2H9v6a1 1 0 1 1-2 0V9H1a1 1 0 0 1 0-2h6V1a1 1 0 0 1 1-1z"/>
      </svg>
      <span>Add assignment</span>
      </button>
    </div>
    <a class="btn btn-danger" href="/logout" role="button">Logout</a>
    {{ !<button type="button" class="btn btn-danger">Logout</button> }}
  </div>
  <div class="row align-items-start">
    <div class="col">
      <ul class="nav nav-tabs position-relative border-0" id="myTab" role="tablist">
        <li class="nav-item" role="presentation">
          <button class="nav-link active border-0" id="students-tab" data-bs-toggle="tab" data-bs-target="#students" type="button" role="tab" aria-controls="students" aria-selected="true">Students</button>
        </li>
        <li class="nav-item" role="presentation">
          <button class="nav-link border-0" id="assignments-tab" data-bs-toggle="tab" data-bs-target="#assignments" type="button" role="tab" aria-controls="assignments" aria-selected="false">Assignments</button>
        </li>
      </ul>
      <div class="tab-content bg-white p-4 shadow rounded" id="myTabContent">
        <div class="tab-pane fade show active" id="students" role="tabpanel" aria-labelledby="students-tab">
          
          <form action="/users/allStudents" method="get">
            <input type="submit" class="btn btn-link p-0" id="getAllStudents" value="Get All Students" />
          </form>

          <div id="students_list">
            <ol class="list-group list-group-numbered list-students">

            {{ #students }}
              <li class="list-group-item d-flex justify-content-between align-items-start p-4">
                <div class="ms-2 me-auto">
                  <h4 class="h4">Student Email: {{ email }}</h4>
                  {{! <p class="lead text-muted"></p> }}
                  <ul class="list-group list-group-flush">
                    {{! <li class="list-group-item pb-3 pt-3">
                      <h6 class="">Assignment One</h6>
                    </li>
                    <li class="list-group-item pb-3 pt-3">
                      <h6 class="">Assignment Two</h6>
                    </li> }}
                  </ul>
                </div>
                <div class="text-center d-flex flex-column align-self-start">
                  <span class="text-muted mb-1">Total</span>
                  <span class="badge bg-success rounded-circle badge-grade d-flex justify-content-center align-items-center">100</span>
                </div>
              </li>
              {{ /students }}
            

            </ol>
          </div>
        </div>
        <div class="tab-pane fade" id="assignments" role="assignments" aria-labelledby="assignments-tab">
          <div class="search-container mb-3 justify-content-end">
            <div class="mb-3 search-all">


              <form action="/assignments" method="get">
                <input type="submit" class="btn btn-link p-0" id="getAllAssignments" value="Get All Assignments" />
              </form>


            </div>
            <div class="mb-3 search-query">


              <form action="/assignments/search" method="get">
              <div class="input-group">
                <input name="title" type="search" class="form-control" placeholder="Search assignments by title" aria-label=Assignment title" aria-describedby="search">
                <input type="submit" class="btn btn-secondary" id="search" value="Search" />
                {{! <button class="btn btn-secondary" type="button" id="search">Search</button> }}
                </div>
              </form>

              {{ #searchResults }}
              <p>Title: {{ title }}</p>
              <p>Content: {{ content }}</p>
              {{ /searchResults }}


            </div>
          </div>
          <div>
            <ol class="list-group list-group-numbered list-non-number">

            {{ #assignments }}
              <li class="list-group-item">
                <div id="assignments_list">
                <div class="d-flex justify-content-between align-items-start pt-3 pb-3">
                  <div class="ms-2 me-auto">
                    <div class="fw-bold">{{ title }}</div>
                    <div>{{ studentEmail }}</div>
                    <div class="mt-2 d-flex">
                      <div class="me-3">
                        <form id="selectGradeForm">
                          <select id="selectGrade" class="form-select rounded border-dark">
                            <option value="none" selected>Grade</option>
                            <option value="100">A</option>
                            <option value="80">B</option>
                            <option value="60">C</option>
                            <option value="40">D</option>
                            <option value="20">E</option>
                            <option value="0">F</option>
                          </select>
                        </form>
                      </div>
                        <div class="me-3 position-relative">
                          <button type="button" id="btnGradeConfirm" class="btn btn-success invisible">Confirm</button>
                          <span id="checkmark" class="badge rounded-pill bg-success invisible checkmark position-absolute top-50 start-0 translate-middle-y text-light d-flex align-items-center justify-content-center">
                            <span>&#10003;</span>
                          </span>
                        </div>
                      </div>
                  </div>
                  <div class="text-center d-flex flex-column justify-content-between">
                    <div class="d-flex align-self-top flex-column">
                      <span class="text-muted">Due date</span>
                      <span class="badge bg-warning badge-pill">{{ dueDate }}</span>
                    </div>
                    <div class="mt-2">
                      <button type="button" class="btn btn-link text-danger">Delete</button>
                    </div>
                  </div>
                  </div>
                </div>
              </li>
              {{ /assignments }}

            </ol>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<div class="modal fade text-dark" id="addAssignment" tabindex="-1" aria-labelledby="addAssignment" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="addAssignment">Assignment</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
      <form action="/assignments" method="post"> 
        {{! <form> }}
          <div class="mb-3">
            <label for="assignment_title" class="form-label">Title</label>
            <input name="title" type="text" class="form-control" id="assignment_title" placeholder="Title">
          </div>
          <div class="mb-3">
            <label for="assignment_description" class="form-label">Description</label>
            <textarea name="content" class="form-control" id="assignment_description" rows="3"></textarea>
          </div>
          <div class="row">
            <div class="col">
              <div class="mb-3">
                <label for="assignment_due" class="form-label">Due Date</label>
                <input name="dueDate" type="text" class="form-control" id="assignment_due" placeholder="ex: Sep, 6">
              </div>
            </div>
            <div class="col">
              <div class="mb-3">
                <label for="assignment_assignee" class="form-label">Assignee</label>
                <select name="studentEmail" id="assignment_assignee" class="form-select form-control rounded">
                  <option selected>Select student</option>
                  <option value="level8@gmai.com">level8@gmai.com</option>
                  <option value="test38@test.com">test38@test.com</option>
                  <option value="student3@email.com">student3@email.com</option>
                </select>
              </div>
            </div>
          </div>

          {{! <div class="form-check form-switch mb-3">
            <input class="form-check-input" type="checkbox" id="flexSwitchCheckDefault">
            <label class="form-check-label" for="flexSwitchCheckDefault">Assign to everyone</label>
          </div> }}
        {{! </form> }}
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
        
        
          <input type="submit" class="btn btn-primary" value="Save changes" />
        </form>

        {{! <button type="button" class="btn btn-primary">Save changes</button> }}
      </div>
    </div>
  </div>
</div>

<script src="/js/teachersView.js"></script>
{{>footer}}
</body>
</html>